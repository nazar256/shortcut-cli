# Vendored OpenAPI spec

`shortcut.openapi.json` is the vendored Shortcut OpenAPI contract used to generate the typed client and derive the raw `shortcut api ...` command surface.

Do not edit generated client files by hand. Refresh the spec intentionally, then run the project generation/test commands before publishing.
